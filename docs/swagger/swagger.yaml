basePath: /
definitions:
  server.breakdownRowResponse:
    properties:
      applied_multiplier:
        type: number
      base_weight:
        type: number
      bias_resistant:
        type: boolean
      contribution:
        type: number
      final_weight:
        type: number
      genre_multipliers:
        additionalProperties:
          format: float64
          type: number
        type: object
      key:
        type: string
      label:
        type: string
      primary_genre:
        type: string
      primary_genre_multiplier:
        type: number
      score:
        type: number
      skipped:
        type: boolean
      weight_override:
        type: boolean
    type: object
  server.dimensionItem:
    properties:
      description:
        description: Description is the scoring hint shown to the user.
        type: string
      key:
        description: Key is the internal identifier used in the scores map of POST
          /score.
        type: string
      label:
        description: Label is the human-readable display name.
        type: string
      weight:
        description: Weight is the base weight for this dimension (all weights sum
          to 1.0).
        type: number
    type: object
  server.dimensionsResponse:
    properties:
      config_hash:
        description: |-
          ConfigHash is the SHA256 digest of the current dimensions config.
          Clients can use this to detect when the dimension list has changed.
        type: string
      dimensions:
        description: Dimensions is the ordered list of scoring dimensions.
        items:
          $ref: '#/definitions/server.dimensionItem'
        type: array
    type: object
  server.errorResponse:
    properties:
      error:
        description: Error is a human-readable description of what went wrong.
        type: string
    type: object
  server.genreMultiplierItem:
    properties:
      genre:
        description: Genre is the AniList genre name (lowercased).
        type: string
      multipliers:
        additionalProperties:
          format: float64
          type: number
        description: Multipliers maps dimension keys to their configured multiplier
          values.
        type: object
    type: object
  server.genresResponse:
    properties:
      genres:
        description: Genres is the list of configured genre multiplier blocks, sorted
          alphabetically.
        items:
          $ref: '#/definitions/server.genreMultiplierItem'
        type: array
      primary_genre_weight:
        description: PrimaryGenreWeight is the configured blend ratio for primary
          genre support.
        type: number
    type: object
  server.mediaResponse:
    properties:
      anilist_url:
        type: string
      average_score:
        type: integer
      chapters:
        type: integer
      cover_image:
        type: string
      episodes:
        type: integer
      format:
        type: string
      genres:
        items:
          type: string
        type: array
      id:
        type: integer
      mean_score:
        type: integer
      media_type:
        type: string
      status:
        type: string
      title_english:
        type: string
      title_native:
        type: string
      title_romaji:
        type: string
    type: object
  server.publishRequest:
    properties:
      media_id:
        description: MediaID is the AniList media ID to publish the score for.
        type: integer
      score:
        description: Score is the final score to publish (e.g. 8.79).
        type: number
    type: object
  server.publishResponse:
    properties:
      message:
        description: Message is a human-readable confirmation.
        type: string
      score:
        description: Score is the score that was written.
        type: number
      title:
        description: Title is the media title as confirmed by AniList.
        type: string
    type: object
  server.scoreRequest:
    properties:
      media_id:
        description: MediaID is the AniList media ID of the entry being scored.
        type: integer
      primary_genre:
        description: |-
          PrimaryGenre designates one of the media's genres as constitutive for
          blended multiplier calculation. Must match one of the media's AniList genres
          (case-insensitive). Optional — omit to use contributing-only averaging with no primary.
        example: Mystery
        type: string
      scores:
        additionalProperties:
          format: float64
          type: number
        description: |-
          Scores maps dimension keys to user scores (1.0–10.0).
          Any configured dimension absent from this map is treated as skipped (N/A).
        type: object
      weight_overrides:
        additionalProperties:
          format: float64
          type: number
        description: |-
          WeightOverrides maps dimension keys to per-session weight overrides.
          Optional — omit to use the weights defined in server config.
        type: object
    type: object
  server.scoreResponse:
    properties:
      breakdown:
        description: Breakdown is the per-dimension audit trail.
        items:
          $ref: '#/definitions/server.breakdownRowResponse'
        type: array
      final_score:
        description: FinalScore is the computed weighted score (rounded to 2 decimal
          places).
        type: number
      meta:
        allOf:
        - $ref: '#/definitions/server.sessionMetaResponse'
        description: Meta carries session-level provenance.
    type: object
  server.sessionMetaResponse:
    properties:
      all_genres:
        items:
          type: string
        type: array
      anilist_url:
        type: string
      config_hash:
        type: string
      matched_genres:
        items:
          type: string
        type: array
      media_id:
        type: integer
      media_type:
        type: string
      primary_genre:
        type: string
      primary_genre_weight:
        type: number
      title_english:
        type: string
      title_romaji:
        type: string
    type: object
host: localhost:8080
info:
  contact:
    name: kansou
  description: Personal anime/manga scoring tool with AniList integration.
  license:
    name: MIT
  title: kansou API
  version: "1.0"
paths:
  /dimensions:
    get:
      description: Returns the ordered list of scoring dimensions defined in server
        config. Use the returned keys in the scores map when calling POST /score.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/server.dimensionsResponse'
      summary: List scoring dimensions
      tags:
      - score
  /genres:
    get:
      description: Returns all configured genre multiplier blocks and the primary
        genre blend ratio.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/server.genresResponse'
      summary: List genre multipliers
      tags:
      - score
  /health:
    get:
      description: Returns 200 OK if the server is running.
      produces:
      - text/plain
      responses:
        "200":
          description: OK
      summary: Health check
      tags:
      - system
  /media/{id}:
    get:
      description: Fetch an anime or manga entry by its AniList media ID.
      parameters:
      - description: AniList media ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/server.mediaResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/server.errorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/server.errorResponse'
        "502":
          description: Bad Gateway
          schema:
            $ref: '#/definitions/server.errorResponse'
      summary: Fetch media by ID
      tags:
      - media
  /media/search:
    get:
      description: Search AniList for anime or manga by name. Returns up to 5 results
        sorted by relevance.
      parameters:
      - description: Search query
        in: query
        name: q
        required: true
        type: string
      - description: 'Media type: ANIME or MANGA'
        in: query
        name: type
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/server.mediaResponse'
            type: array
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/server.errorResponse'
        "404":
          description: Not Found
          schema:
            $ref: '#/definitions/server.errorResponse'
        "502":
          description: Bad Gateway
          schema:
            $ref: '#/definitions/server.errorResponse'
      summary: Search media
      tags:
      - media
  /score:
    post:
      consumes:
      - application/json
      description: Calculate a weighted, genre-adjusted score for a media entry.
      parameters:
      - description: Scoring input
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/server.scoreRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/server.scoreResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/server.errorResponse'
        "502":
          description: Bad Gateway
          schema:
            $ref: '#/definitions/server.errorResponse'
      summary: Calculate score
      tags:
      - score
  /score/publish:
    post:
      consumes:
      - application/json
      description: Write a score to the user's AniList account. Requires ANILIST_TOKEN
        env var.
      parameters:
      - description: Publish input
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/server.publishRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/server.publishResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/server.errorResponse'
        "502":
          description: Bad Gateway
          schema:
            $ref: '#/definitions/server.errorResponse'
      summary: Publish score
      tags:
      - score
swagger: "2.0"
