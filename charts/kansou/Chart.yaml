apiVersion: v2
name: kansou
description: Anime/manga scoring server
type: application
version: 0.1.0       # chart version — bump this when the chart changes
appVersion: "0.1.0"  # application version — matches the kansou binary release tag
