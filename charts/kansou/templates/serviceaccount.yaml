{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kansou.serviceAccountName" . }}
  labels:
    {{- include "kansou.labels" . | nindent 4 }}
{{- end }}
