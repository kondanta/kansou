{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kansou.fullname" . }}
  labels:
    {{- include "kansou.labels" . | nindent 4 }}
data:
  config.toml: |
    {{- .Values.config | nindent 4 }}
{{- end }}
